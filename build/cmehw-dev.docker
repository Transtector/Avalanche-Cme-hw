# This docker is used for a cmehw developer image.
#
# Adds Python libraries, external toolsets, and headers
# required to build the cmehw application.

# Uses the base-alpine-python (2.7) image
FROM cuscsgit01.smiths.net:5000/base-alpine-python

MAINTAINER James Brunner <james.brunner@smithsmicrowave.com>

# Add build tools, headers, and Python dev tools
RUN apk add --update \
	build-base \
	linux-headers \
	rrdtool-dev \
	python-dev 

# Add the source code folder (will sit in /root)
VOLUME Cme-hw



CMD ["/bin/bash"]
